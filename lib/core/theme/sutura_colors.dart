import 'package:flutter/material.dart';

class SuturaColors {
  // 🌿 Primary Palette
  static const primary = Color(0xFF0C3B2E); // forest green
  static const secondary = Color(0xFF6D9773); // sage green
  static const accent = Color(0xFFFFBA00); // gold

  // 🧻 Background & Surface
  static const background = Color(0xFFF3F8F4); // soft mint
  static const card = Colors.white;

  // 🖋 Text
  static const textPrimary = Color(0xFF0C3B2E);
  static const textSecondary = Color(0xFF6B7F76);

  // 🔔 Status
  static const success = Color(0xFF6D9773);
  static const danger = Color(0xFFB46617); // earthy brown

  // Accent
  static const Color accentPurple = Color(0xFFE6D9FF);
  static const Color accentLime = Color(0xFFB9F36A);
  static const Color accentMint = Color(0xFF7FE0C3);

  // Border / Divider
  static const Color border = Color(0xFFE2E8F0);
}
